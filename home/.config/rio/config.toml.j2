confirm-before-quit = false
draw-bold-text-with-light-colors = true
hide-cursor-when-typing = true
ignore-selection-foreground-color = true
option-as-alt = "Left"
theme = "Nord"

# XDG Base Directory Specification
env-vars = [
  "XDG_BIN_HOME={{ guisu.dstDir }}/.local/bin",
  "XDG_CACHE_HOME={{ guisu.dstDir }}/.cache",
  "XDG_CONFIG_HOME={{ guisu.dstDir }}/.config",
  "XDG_DATA_HOME={{ guisu.dstDir }}/.local/share",
  "XDG_PROJECTS_DIR={{ guisu.dstDir }}/Projects",
  "XDG_STATE_HOME={{ guisu.dstDir }}/.local/state",
]

# [adaptive-theme]
# dark = "Nord"
# light = "Nord-Light"

[bindings]
keys = [
  { key = "q", with = "super", action = "Quit" },
  { key = "return", with = "shift", esc = "\n" },
  { key = "return", with = "super", action = "ToggleFullscreen" }
]

[cursor]
blinking = true
shape = "Block"

[editor]
program = "nvim"

[fonts]
family = "{{ visual.fontFamily }}"
features = ["cv31", "cv38", "cv61", "cv97", "ss03", "ss07", "ss08", "ss10", "zero"]
size = {{ visual.terminal.fontSize }}
use-drawable-chars = false

[navigation]
clickable = true
hide-if-single = true
mode = "Bookmark"
unfocused-split-opacity = 0.9
use-current-path = true

[shell]
program = "{{ terminal.rio.shell }}"
args = ["--login"]

[window]
blur = true
decorations = "Transparent"
macos-use-unified-titlebar = true
mode = "Maximized"
opacity = 0.7
